// src/components/BeginWorkoutButton.jsx
import React from 'react';

const BeginWorkoutButton = ({ onClick }) => {
  return <button onClick={onClick}>Begin Workout</button>;
};

export default BeginWorkoutButton;